# TEMPLATE LATEX SKRIPSI GUNADARMA IF


## Install di Fedora 

dependencies nya: 
```bash
sudo dnf install texlive-harvard
sudo dnf install texlive-babel-indonesian texlive-hyphen-indonesian
```

editor nya:
```bash
sudo dnf install texstudio
```

## Saran bib reference manager:
  - JabRef
 ```bash
  flatpak install flathub org.jabref.jabref
 ```